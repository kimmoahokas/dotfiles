plugins=(mosh sublime brew osx)

export PATH=$PATH:/opt/local/bin:/opt/local/sbin:/Applications/eclipse/android-sdk-macosx/platform-tools:/Applications/eclipse/android-sdk-macosx/tools

# fix äö and such in tab completion
setopt combiningchars

alias irc='ssh lelulaatikko -t tmux attach -t irssi'

#make sure that ~/.bin is in path and then link subl there
# ln -s "/Applications/Sublime Text.app/Contents/SharedSupport/bin/subl" ~/bin/subl
EDITOR="subl -w"
VISUAL="$EDITOR"
